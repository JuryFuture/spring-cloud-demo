server:
  port: 8000
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JuryFuture/config.git
          username: JuryFuture
          password: JuryFuture0
